# Security & Ethics Prompt (Minimal for MVP)

- Respect ownership/consent of voices when using cloning (XTTS reference samples).
- Limit max upload size, virus-scan stub.
- Signed URLs for downloads; private buckets by default.
- Redact PII from logs.
- Feature-flag any future content moderation (out of scope for MVP).

Deliver minimal policies in `docs/` and a config-driven kill-switch for cloning.
